export default function HomePage() {
  return (
    <main className="page-shell">
      <section className="status-card" aria-labelledby="foundation-title">
        <p className="eyebrow">CrossVal Plan vs Actual Tracker</p>
        <h1 id="foundation-title">Monorepo foundation ready</h1>
        <p>
          The Next.js frontend, Express API, shared contracts package, and TanStack Query provider
          are connected. Product implementation begins in later roadmap phases.
        </p>
      </section>
    </main>
  );
}
